# Copy this file to "keystore.properties" (already git-ignored - never commit the real one)
# and fill in the values from the keystore you generated with `keytool -genkeypair`.
#
# storeFile can be an absolute path, or relative to the project root.

storeFile=llm-studio-release.jks
storePassword=CHANGE_ME
keyAlias=llm-studio
keyPassword=CHANGE_ME
